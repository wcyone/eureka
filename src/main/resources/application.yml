#eureka 是springcloud的注册中心，和zk一样
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: false
  server:
    enable-self-preservation: false   #开发环境下设置自我保护  上线则不写 默认为true
#注册中心的名字，服务名称
spring:
  application:
    name: eureka
server:
  port: 8761
